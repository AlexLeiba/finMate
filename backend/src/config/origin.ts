export const corsOptions = {
  origin: ["http://localhost:5173"], //place public production frontend url here
  credentials: true, //allowing cookies sending from frontend to backend of allowed origins (per request)
};
